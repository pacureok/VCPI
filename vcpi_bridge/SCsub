# SCsub para el módulo VCPI (Pacure AI Labs Pro)
Import('env')

# Añadimos los archivos fuente del puente
sources = [
    "register_types.cpp",
    "vcpi_bridge.cpp"
]

# Registramos el módulo en el entorno de construcción de Godot
env.add_source_files(env.modules_sources, sources)
